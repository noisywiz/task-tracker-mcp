# Task Tracker MCP Server Configuration
# Copy this file to .env and customize for your environment

# Transport mode: stdio, http, or streamable-http
# Use 'http' for persistent daemon mode
MCP_TRANSPORT=http

# Host to bind to (for HTTP transports)
# Use 0.0.0.0 to listen on all interfaces
# Use 127.0.0.1 for localhost only
MCP_HOST=0.0.0.0

# Port for the server (for HTTP transports)
# Change this if port 8000 is already in use
TASK_TRACKER_MCP_PORT=8000

# Python logging behavior
# Set to 1 to show logs in real-time (recommended)
PYTHONUNBUFFERED=1

# Docker host port mapping
# Change this to use a different port on your host machine
# Format: HOST_PORT:CONTAINER_PORT
# Example: 9000:8000 would map port 9000 on your host to 8000 in container
DOCKER_PORT=8000
